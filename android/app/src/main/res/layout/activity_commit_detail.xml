<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/commit_detail_linearlayout_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <!-- Commit Author's Name -->
    <TextView
        android:id="@+id/commit_detail_textview_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        tools:text="commit author" />

    <!-- Commit Author's Email Address -->
    <TextView
        android:id="@+id/commit_detail_textview_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        tools:text="commit author email" />

    <!-- Commit Date -->
    <TextView
        android:id="@+id/commit_detail_textview_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        tools:text="commit date" />

    <!-- Commit Message -->
    <TextView
        android:id="@+id/commit_detail_textview_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        tools:text="commit message" />

</LinearLayout>
